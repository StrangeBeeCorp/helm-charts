apiVersion: v1
kind: Service
metadata:
  labels:
    app: cortex
  name: cortex
  namespace: {{ .Release.Namespace }}
spec:
  type: LoadBalancer
  ports:
    - port: {{ .Values.cortex.service.port }}
  selector:
    app: cortex